{
  "name": "example-basic",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && vitest run --config vitest.config.report.ts"
  },
  "devDependencies": {
    "typescript": "latest",
    "vitest": "latest",
    "vitest-sonar-reporter": "file:../../"
  }
}
