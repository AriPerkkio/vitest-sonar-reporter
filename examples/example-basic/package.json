{
    "name": "example-basic",
    "private": true,
    "scripts": {
        "test": "vitest run && vitest run --config vitest.config.report.ts"
    },
    "devDependencies": {
        "typescript": "latest",
        "vite": "^3.1",
        "vitest": "latest",
        "vitest-sonar-reporter": "file:../../"
    }
}
